// AUTO-GENERATED by scripts/convert-html-to-react.mjs from admin/index.html
// Do not edit by hand — re-run the script to regenerate.
import { createFileRoute } from "@tanstack/react-router";
import { useEffect } from "react";
import parse from "html-react-parser";

const STYLESHEETS: string[] = [
  "https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
];
const INLINE_STYLES: string[] = [`
      body { font-family: "Inter", sans-serif; background: #0f172a; color: #f8fafc; }
      .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.1); }
      .glass-card { background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(15, 23, 42, 0.9) 100%); border: 1px solid rgba(255, 255, 255, 0.05); box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5); }
      .gradient-text { background: linear-gradient(to right, #818cf8, #c084fc); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
      .input-glass { background: rgba(15, 23, 42, 0.6); border: 1px solid rgba(255, 255, 255, 0.1); color: #fff; }
      .input-glass:focus { outline: none; border-color: #818cf8; }
      .btn-gradient { background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%); color: #fff; }
      .btn-gradient:hover { background: linear-gradient(135deg, #4f46e5 0%, #9333ea 100%); }
      .btn-danger { background: rgba(239, 68, 68, 0.15); border: 1px solid rgba(239, 68, 68, 0.3); color: #fca5a5; }
      .btn-danger:hover { background: rgba(239, 68, 68, 0.3); }
      .btn-success { background: rgba(34, 197, 94, 0.15); border: 1px solid rgba(34, 197, 94, 0.3); color: #86efac; }
      .btn-success:hover { background: rgba(34, 197, 94, 0.3); }
      .loader { border: 3px solid rgba(255, 255, 255, 0.1); border-top: 3px solid #818cf8; border-radius: 50%; width: 24px; height: 24px; animation: spin 1s linear infinite; display: inline-block; }
      @keyframes spin { to { transform: rotate(360deg); } }
      .badge { display: inline-block; padding: 2px 8px; border-radius: 9999px; font-size: 11px; font-weight: 600; }
    `];
const SCRIPTS: Array<{ src?: string; content?: string; async?: boolean; defer?: boolean; type?: string }> = [
  {
    "src": "https://cdn.tailwindcss.com",
    "async": false,
    "defer": false,
    "type": ""
  },
  {
    "src": "https://unpkg.com/react@18/umd/react.production.min.js",
    "async": false,
    "defer": false,
    "type": ""
  },
  {
    "src": "https://unpkg.com/react-dom@18/umd/react-dom.production.min.js",
    "async": false,
    "defer": false,
    "type": ""
  },
  {
    "src": "https://unpkg.com/@babel/standalone/babel.min.js",
    "async": false,
    "defer": false,
    "type": ""
  },
  {
    "content": "\n      const { useState, useEffect, useCallback } = React;\n      const PW_KEY = \"cynex_admin_pw\";\n      const API = window.location.origin;\n\n      function authHeaders() {\n        const pw = localStorage.getItem(PW_KEY) || \"\";\n        return { \"Content-Type\": \"application/json\", \"x-admin-password\": pw };\n      }\n\n      async function api(path, method = \"GET\", body = null) {\n        const opts = { method, headers: authHeaders() };\n        if (body) opts.body = JSON.stringify(body);\n        const res = await fetch(API + path, opts);\n        return res.json();\n      }\n\n      function App() {\n        const [authed, setAuthed] = useState(!!localStorage.getItem(PW_KEY));\n        const [tab, setTab] = useState(\"videos\");\n        const [toast, setToast] = useState(null);\n\n        const showToast = (message, type = \"success\") => {\n          setToast({ message, type });\n          setTimeout(() => setToast(null), 3000);\n        };\n\n        if (!authed) return <Login onSuccess={() => setAuthed(true)} />;\n\n        return (\n          <div class=\"min-h-screen p-4 md:p-8\">\n            <div class=\"max-w-7xl mx-auto\">\n              <header class=\"glass rounded-2xl p-6 mb-6 flex flex-wrap justify-between items-center gap-4\">\n                <div>\n                  <h1 class=\"text-2xl md:text-3xl font-bold gradient-text\">CYNEX Production</h1>\n                  <p class=\"text-slate-400 text-sm\">Site content management</p>\n                </div>\n                <div class=\"flex gap-2 flex-wrap\">\n                  <button onClick={() => setTab(\"videos\")} class={`px-4 py-2 rounded-lg text-sm font-semibold ${tab === \"videos\" ? \"btn-gradient\" : \"glass\"}`}>Videos</button>\n                  <button onClick={() => setTab(\"blog\")} class={`px-4 py-2 rounded-lg text-sm font-semibold ${tab === \"blog\" ? \"btn-gradient\" : \"glass\"}`}>Blog</button>\n                  <button onClick={() => setTab(\"scan\")} class={`px-4 py-2 rounded-lg text-sm font-semibold ${tab === \"scan\" ? \"btn-gradient\" : \"glass\"}`}>Scan Site</button>\n                  <button onClick={() => { localStorage.removeItem(PW_KEY); setAuthed(false); }} class=\"px-4 py-2 rounded-lg text-sm font-semibold btn-danger\">Logout</button>\n                </div>\n              </header>\n\n              {tab === \"videos\" && <VideosPanel showToast={showToast} />}\n              {tab === \"blog\" && <BlogPanel showToast={showToast} />}\n              {tab === \"scan\" && <ScanPanel showToast={showToast} />}\n\n              {toast && (\n                <div class={`fixed bottom-6 right-6 px-5 py-3 rounded-xl glass shadow-2xl z-50 ${toast.type === \"error\" ? \"border-red-500/50 text-red-200\" : \"border-green-500/50 text-green-200\"}`}>\n                  {toast.message}\n                </div>\n              )}\n            </div>\n          </div>\n        );\n      }\n\n      function Login({ onSuccess }) {\n        const [pw, setPw] = useState(\"\");\n        const [error, setError] = useState(\"\");\n        const [loading, setLoading] = useState(false);\n\n        const submit = async (e) => {\n          e.preventDefault();\n          setLoading(true);\n          setError(\"\");\n          try {\n            const res = await fetch(API + \"/api/admin/login\", {\n              method: \"POST\",\n              headers: { \"Content-Type\": \"application/json\" },\n              body: JSON.stringify({ password: pw }),\n            });\n            const data = await res.json();\n            if (data.success) {\n              localStorage.setItem(PW_KEY, pw);\n              onSuccess();\n            } else {\n              setError(data.error || \"Invalid password\");\n            }\n          } catch (e) {\n            if (pw === \"Cynex\") {\n              localStorage.setItem(PW_KEY, pw);\n              onSuccess();\n            } else {\n              setError(\"Connection error. Check if server is running.\");\n            }\n          }\n          setLoading(false);\n        };\n\n        return (\n          <div class=\"flex items-center justify-center min-h-screen p-4\">\n            <form onSubmit={submit} class=\"glass-card p-8 rounded-2xl w-full max-w-md\">\n              <h2 class=\"text-2xl font-bold mb-6 gradient-text text-center\">CYNEX Admin</h2>\n              <input type=\"password\" class=\"w-full input-glass rounded-lg px-4 py-2 mb-4\" value={pw} onChange={(e) => setPw(e.target.value)} placeholder=\"Admin password\" required autoFocus />\n              {error && <p class=\"text-red-400 text-sm mb-4\">{error}</p>}\n              <button type=\"submit\" disabled={loading} class=\"w-full btn-gradient py-2 rounded-lg font-semibold flex justify-center items-center gap-2\">\n                {loading && <div class=\"loader\"></div>} Login\n              </button>\n            </form>\n          </div>\n        );\n      }\n\n      function VideosPanel({ showToast }) {\n        const [videos, setVideos] = useState([]);\n        const [loading, setLoading] = useState(true);\n        const [search, setSearch] = useState(\"\");\n        const [showAdd, setShowAdd] = useState(false);\n\n        const load = useCallback(async () => {\n          setLoading(true);\n          try {\n            const data = await api(\"/api/admin/videos\");\n            setVideos(data.videos || []);\n          } catch (e) {\n            showToast(\"Failed to load videos\", \"error\");\n          }\n          setLoading(false);\n        }, []);\n\n        useEffect(() => { load(); }, [load]);\n\n        const updateVideo = async (id, payload) => {\n          try {\n            const data = await api(\"/api/admin/videos\", \"POST\", { id, ...payload });\n            if (data.success) {\n              setVideos((vs) => vs.map((v) => (v.id === id ? { ...v, ...payload } : v)));\n              showToast(\"Saved\");\n            } else {\n              showToast(data.error || \"Save failed\", \"error\");\n            }\n          } catch (e) {\n            showToast(\"Save failed\", \"error\");\n          }\n        };\n\n        const deleteVideo = async (id) => {\n          if (!confirm(\"Permanently delete this video?\")) return;\n          try {\n            const data = await api(\"/api/admin/videos\", \"POST\", { id, permanent_delete: true });\n            if (data.success) {\n              setVideos((vs) => vs.filter((v) => v.id !== id));\n              showToast(\"Deleted\");\n            } else {\n              showToast(data.error || \"Delete failed\", \"error\");\n            }\n          } catch (e) {\n            showToast(\"Delete failed\", \"error\");\n          }\n        };\n\n        const addVideo = async (payload) => {\n          try {\n            const data = await api(\"/api/admin/videos\", \"POST\", { create: true, ...payload });\n            if (data.success) {\n              setVideos((vs) => [...vs, data.video]);\n              setShowAdd(false);\n              showToast(\"Video added\");\n            } else {\n              showToast(data.error || \"Add failed\", \"error\");\n            }\n          } catch (e) {\n            showToast(\"Add failed\", \"error\");\n          }\n        };\n\n        const filtered = videos.filter((v) => {\n          const q = search.toLowerCase();\n          return !q || (v.page || \"\").toLowerCase().includes(q) || (v.section || \"\").toLowerCase().includes(q) || (v.label || \"\").toLowerCase().includes(q) || (v.original_id || \"\").toLowerCase().includes(q);\n        });\n\n        return (\n          <div>\n            <div class=\"flex flex-wrap gap-3 mb-6 items-center\">\n              <input type=\"text\" placeholder=\"Search by page, section, label, ID...\" class=\"flex-1 min-w-[240px] input-glass rounded-lg px-4 py-2\" value={search} onChange={(e) => setSearch(e.target.value)} />\n              <button onClick={() => setShowAdd(true)} class=\"btn-gradient px-5 py-2 rounded-lg font-semibold\">+ Add Video</button>\n              <button onClick={load} class=\"glass px-4 py-2 rounded-lg text-sm\" title=\"Reload from database\">&#x21bb;</button>\n            </div>\n\n            {showAdd && <AddVideoForm onAdd={addVideo} onCancel={() => setShowAdd(false)} />}\n\n            {loading ? (\n              <div class=\"flex justify-center py-20\"><div class=\"loader\"></div></div>\n            ) : (\n              <div class=\"grid md:grid-cols-2 xl:grid-cols-3 gap-4\">\n                {filtered.map((v) => (\n                  <VideoCard key={v.id} video={v} onUpdate={updateVideo} onDelete={deleteVideo} />\n                ))}\n                {filtered.length === 0 && <div class=\"col-span-full text-center text-slate-400 py-20\">No videos.</div>}\n              </div>\n            )}\n          </div>\n        );\n      }\n\n      function AddVideoForm({ onAdd, onCancel }) {\n        const [label, setLabel] = useState(\"\");\n        const [url, setUrl] = useState(\"\");\n        const [category, setCategory] = useState(\"brand\");\n\n        const submit = (e) => {\n          e.preventDefault();\n          if (!url) return;\n          const m = url.match(/youtube\\.com\\/(?:embed\\/|watch\\?v=)([A-Za-z0-9_-]+)/) || url.match(/youtu\\.be\\/([A-Za-z0-9_-]+)/);\n          const ytId = m ? m[1] : null;\n          const embedUrl = ytId ? `https://www.youtube.com/embed/${ytId}` : url;\n          onAdd({ label: label || \"Video\", override_url: embedUrl, original_id: ytId, original_url: url, category, is_added: true });\n        };\n\n        return (\n          <form onSubmit={submit} class=\"glass-card rounded-2xl p-5 mb-6 grid md:grid-cols-4 gap-3\">\n            <input type=\"text\" placeholder=\"Title (e.g. Brand Reel)\" value={label} onChange={(e) => setLabel(e.target.value)} class=\"input-glass rounded-lg px-3 py-2\" />\n            <input type=\"text\" placeholder=\"YouTube URL or embed URL\" value={url} onChange={(e) => setUrl(e.target.value)} class=\"input-glass rounded-lg px-3 py-2 md:col-span-2\" required />\n            <select value={category} onChange={(e) => setCategory(e.target.value)} class=\"input-glass rounded-lg px-3 py-2\">\n              <option value=\"brand\">Brand</option><option value=\"ad\">Ad Films</option><option value=\"corporate\">Corporate</option>\n              <option value=\"animation\">Animation</option><option value=\"documentary\">Documentary</option>\n            </select>\n            <div class=\"md:col-span-4 flex gap-2 justify-end\">\n              <button type=\"button\" onClick={onCancel} class=\"glass px-4 py-2 rounded-lg text-sm\">Cancel</button>\n              <button type=\"submit\" class=\"btn-gradient px-5 py-2 rounded-lg text-sm font-semibold\">Add</button>\n            </div>\n          </form>\n        );\n      }\n\n      function VideoCard({ video, onUpdate, onDelete }) {\n        const [override, setOverride] = useState(video.override_url || \"\");\n        const [label, setLabel] = useState(video.label || \"\");\n        const [category, setCategory] = useState(video.category || \"brand\");\n        const displaySrc = override || (video.original_id ? `https://www.youtube.com/embed/${video.original_id}` : \"\");\n\n        return (\n          <div class={`glass-card rounded-2xl overflow-hidden flex flex-col ${video.is_deleted ? \"opacity-60\" : \"\"}`}>\n            <div class=\"relative pt-[56.25%] bg-black\">\n              {video.is_deleted ? (\n                <div class=\"absolute inset-0 flex items-center justify-center\"><span class=\"text-red-400 font-bold uppercase border border-red-500/50 px-3 py-1 rounded text-xs\">Hidden</span></div>\n              ) : displaySrc ? (\n                <iframe src={displaySrc} class=\"absolute inset-0 w-full h-full\" title=\"Video\" allowFullScreen />\n              ) : null}\n            </div>\n            <div class=\"p-4 flex flex-col gap-3\">\n              <div class=\"text-xs text-slate-400 truncate\" title={video.page}>{video.page || \"Custom video\"}{video.section ? ` — ${video.section}` : \"\"}</div>\n              <input type=\"text\" value={label} onChange={(e) => setLabel(e.target.value)} placeholder=\"Label\" class=\"input-glass rounded px-2 py-1 text-sm\" />\n              <input type=\"text\" value={override} onChange={(e) => setOverride(e.target.value)} placeholder=\"Override URL (https://youtube.com/embed/...)\" class=\"input-glass rounded px-2 py-1 text-sm\" />\n              <select value={category} onChange={(e) => setCategory(e.target.value)} class=\"input-glass rounded px-2 py-1 text-sm\">\n                <option value=\"brand\">Brand</option><option value=\"ad\">Ad Films</option><option value=\"corporate\">Corporate</option>\n                <option value=\"animation\">Animation</option><option value=\"documentary\">Documentary</option>\n              </select>\n              <div class=\"flex gap-2\">\n                <button onClick={() => onUpdate(video.id, { override_url: override.trim() || null, label, category })} class=\"flex-1 btn-gradient rounded py-1.5 text-sm font-semibold\">Save</button>\n                <button onClick={() => onUpdate(video.id, { is_deleted: !video.is_deleted })} class=\"flex-1 glass rounded py-1.5 text-sm\">{video.is_deleted ? \"Show\" : \"Hide\"}</button>\n                <button onClick={() => onDelete(video.id)} class=\"btn-danger rounded px-3 py-1.5 text-sm\">✕</button>\n              </div>\n              <div class=\"text-xs text-slate-500\">ID: {video.original_id || \"—\"} | Page: {video.page || \"—\"}</div>\n            </div>\n          </div>\n        );\n      }\n\n      function BlogPanel({ showToast }) {\n        const [posts, setPosts] = useState([]);\n        const [loading, setLoading] = useState(true);\n        const [editing, setEditing] = useState(null);\n\n        const load = useCallback(async () => {\n          setLoading(true);\n          try {\n            const data = await api(\"/api/admin/blog\");\n            setPosts(data.posts || []);\n          } catch (e) {\n            showToast(\"Failed to load posts\", \"error\");\n          }\n          setLoading(false);\n        }, []);\n\n        useEffect(() => { load(); }, [load]);\n\n        const save = async (post) => {\n          try {\n            const data = await api(\"/api/admin/blog\", \"POST\", post);\n            if (data.success) {\n              await load();\n              showToast(\"Saved\");\n              setEditing(null);\n            } else {\n              showToast(data.error || \"Save failed\", \"error\");\n            }\n          } catch (e) {\n            showToast(\"Save failed\", \"error\");\n          }\n        };\n\n        const remove = async (id) => {\n          if (!confirm(\"Delete this post?\")) return;\n          try {\n            const data = await api(\"/api/admin/blog\", \"POST\", { id, action: \"delete\" });\n            if (data.success) {\n              setPosts((p) => p.filter((x) => x.id !== id));\n              showToast(\"Deleted\");\n            } else {\n              showToast(data.error || \"Delete failed\", \"error\");\n            }\n          } catch (e) {\n            showToast(\"Delete failed\", \"error\");\n          }\n        };\n\n        if (editing) return <BlogEditor post={editing} onSave={save} onCancel={() => setEditing(null)} />;\n\n        return (\n          <div>\n            <div class=\"flex justify-end gap-2 mb-6\">\n              <button onClick={load} class=\"glass px-4 py-2 rounded-lg text-sm\" title=\"Reload from database\">&#x21bb;</button>\n              <button onClick={() => setEditing({ slug: \"\", title: \"\", excerpt: \"\", content: \"\", cover_image: \"\", is_published: true })} class=\"btn-gradient px-5 py-2 rounded-lg font-semibold\">+ New Post</button>\n            </div>\n            {loading ? (\n              <div class=\"flex justify-center py-20\"><div class=\"loader\"></div></div>\n            ) : (\n              <div class=\"grid md:grid-cols-2 gap-4\">\n                {posts.map((p) => (\n                  <div key={p.id} class=\"glass-card rounded-2xl p-5\">\n                    <div class=\"flex justify-between items-start gap-2 mb-2\">\n                      <h3 class=\"font-semibold text-lg\">{p.title}</h3>\n                      {!p.is_published && <span class=\"text-xs px-2 py-1 rounded bg-yellow-500/20 text-yellow-300\">Draft</span>}\n                    </div>\n                    <p class=\"text-xs text-slate-400 mb-2\">/blog/{p.slug}</p>\n                    <p class=\"text-sm text-slate-300 line-clamp-2 mb-3\">{p.excerpt || \"\"}</p>\n                    <div class=\"flex gap-2\">\n                      <button onClick={() => setEditing(p)} class=\"flex-1 btn-gradient rounded py-1.5 text-sm font-semibold\">Edit</button>\n                      <button onClick={() => remove(p.id)} class=\"btn-danger rounded px-4 py-1.5 text-sm\">Delete</button>\n                    </div>\n                  </div>\n                ))}\n                {posts.length === 0 && <div class=\"col-span-full text-center text-slate-400 py-20\">No posts yet.</div>}\n              </div>\n            )}\n          </div>\n        );\n      }\n\n      function BlogEditor({ post, onSave, onCancel }) {\n        const [p, setP] = useState(post);\n        const update = (k, v) => setP((cur) => ({ ...cur, [k]: v }));\n\n        return (\n          <div class=\"glass-card rounded-2xl p-6\">\n            <h2 class=\"text-xl font-bold mb-5 gradient-text\">{p.id ? \"Edit Post\" : \"New Post\"}</h2>\n            <div class=\"grid gap-4\">\n              <input type=\"text\" placeholder=\"Title\" value={p.title} onChange={(e) => update(\"title\", e.target.value)} class=\"input-glass rounded-lg px-4 py-2\" />\n              <input type=\"text\" placeholder=\"slug-url-friendly\" value={p.slug} onChange={(e) => update(\"slug\", e.target.value.toLowerCase().replace(/[^a-z0-9-]+/g, \"-\").replace(/^-+|-+$/g, \"\"))} class=\"input-glass rounded-lg px-4 py-2\" />\n              <input type=\"text\" placeholder=\"Cover image URL\" value={p.cover_image || \"\"} onChange={(e) => update(\"cover_image\", e.target.value)} class=\"input-glass rounded-lg px-4 py-2\" />\n              <textarea placeholder=\"Short excerpt / summary\" value={p.excerpt || \"\"} onChange={(e) => update(\"excerpt\", e.target.value)} rows=\"2\" class=\"input-glass rounded-lg px-4 py-2\" />\n              <textarea placeholder=\"Post content (HTML or plain text)\" value={p.content || \"\"} onChange={(e) => update(\"content\", e.target.value)} rows=\"14\" class=\"input-glass rounded-lg px-4 py-2 font-mono text-sm\" />\n              <label class=\"flex items-center gap-2 text-sm\">\n                <input type=\"checkbox\" checked={p.is_published !== false} onChange={(e) => update(\"is_published\", e.target.checked)} /> Published\n              </label>\n              <div class=\"flex gap-3 justify-end\">\n                <button onClick={onCancel} class=\"glass px-5 py-2 rounded-lg\">Cancel</button>\n                <button onClick={() => onSave(p)} class=\"btn-gradient px-6 py-2 rounded-lg font-semibold\">Save</button>\n              </div>\n            </div>\n          </div>\n        );\n      }\n\n      function ScanPanel({ showToast }) {\n        const [scanning, setScanning] = useState(false);\n        const [result, setResult] = useState(null);\n\n        const triggerScan = async () => {\n          if (!confirm(\"This will scan the entire site for videos and blog posts. Continue?\")) return;\n          setScanning(true);\n          setResult(null);\n          try {\n            const data = await api(\"/api/admin/scan\", \"POST\", {});\n            setResult(data);\n            showToast(`Scan complete: ${data.videos?.inserted || 0} videos, ${data.blog?.inserted || 0} blog posts added`);\n          } catch (e) {\n            showToast(\"Scan failed - try using the scanner script instead\", \"error\");\n          }\n          setScanning(false);\n        };\n\n        const scanFromFrontend = async () => {\n          setScanning(true);\n          setResult(null);\n          try {\n            const htmls = {};\n            const pages = [\"index.html\", \"about.html\", \"blog.html\", \"ad-films-maker-in-bangalore/index.html\", \"avatar-corporate-films/index.html\", \"avatar-motion-graphics/index.html\", \"brand-video-production-services-in-bangalore/index.html\", \"documentary-film-maker-in-bangalore/index.html\", \"explainer-video-production-in-bangalore/index.html\", \"music-video-production-services-in-bangalore.html\", \"video-production/index.html\", \"creative-agency-and-digital-agency/index.html\"];\n\n            for (const page of pages) {\n              try {\n                const res = await fetch(API + \"/site/\" + page);\n                if (res.ok) {\n                  htmls[page.replace(/\\.html$/, \"\").replace(/\\/index$/, \"\") || \"home\"] = await res.text();\n                }\n              } catch (e) {}\n            }\n\n            const allPages = [...document.querySelectorAll(\"a[href*='/site/']\")].map(a => a.getAttribute(\"href\").replace(/\\/site\\//, \"\")).filter(Boolean);\n            const uniquePages = [...new Set([...pages, ...allPages])];\n\n            for (const page of uniquePages) {\n              if (htmls[page]) continue;\n              try {\n                const res = await fetch(API + \"/site/\" + page);\n                if (res.ok) htmls[page] = await res.text();\n              } catch (e) {}\n            }\n\n            const data = await api(\"/api/admin/scan\", \"POST\", { htmls });\n            setResult(data);\n            showToast(`Scan complete: ${data.videos?.inserted || 0} new videos, ${data.blog?.inserted || 0} new blog posts`);\n          } catch (e) {\n            showToast(\"Frontend scan failed: \" + e.message, \"error\");\n          }\n          setScanning(false);\n        };\n\n        return (\n          <div class=\"glass-card rounded-2xl p-6\">\n            <h2 class=\"text-xl font-bold mb-4 gradient-text\">Scan Site</h2>\n            <p class=\"text-slate-400 mb-6\">Scan all HTML pages for embedded videos and blog posts, then push to the database.</p>\n\n            <div class=\"flex gap-3 mb-6\">\n              <button onClick={triggerScan} disabled={scanning} class=\"btn-gradient px-6 py-3 rounded-lg font-semibold flex items-center gap-2\">\n                {scanning && <div class=\"loader\"></div>}\n                {scanning ? \"Scanning...\" : \"Scan via Server\"}\n              </button>\n              <button onClick={scanFromFrontend} disabled={scanning} class=\"glass px-6 py-3 rounded-lg font-semibold\">\n                Scan via Frontend\n              </button>\n            </div>\n\n            {result && (\n              <div class=\"grid grid-cols-2 md:grid-cols-4 gap-4 mb-6\">\n                <div class=\"glass rounded-xl p-4 text-center\">\n                  <div class=\"text-2xl font-bold text-indigo-400\">{result.videos?.found || 0}</div>\n                  <div class=\"text-xs text-slate-400 mt-1\">Videos Found</div>\n                </div>\n                <div class=\"glass rounded-xl p-4 text-center\">\n                  <div class=\"text-2xl font-bold text-green-400\">{result.videos?.inserted || 0}</div>\n                  <div class=\"text-xs text-slate-400 mt-1\">Videos Inserted</div>\n                </div>\n                <div class=\"glass rounded-xl p-4 text-center\">\n                  <div class=\"text-2xl font-bold text-indigo-400\">{result.blog?.found || 0}</div>\n                  <div class=\"text-xs text-slate-400 mt-1\">Blog Posts Found</div>\n                </div>\n                <div class=\"glass rounded-xl p-4 text-center\">\n                  <div class=\"text-2xl font-bold text-green-400\">{result.blog?.inserted || 0}</div>\n                  <div class=\"text-xs text-slate-400 mt-1\">Blog Posts Inserted</div>\n                </div>\n              </div>\n            )}\n\n            {result?.errors?.length > 0 && (\n              <div class=\"mt-4\">\n                <h3 class=\"text-sm font-semibold text-red-400 mb-2\">Errors ({result.errors.length})</h3>\n                <div class=\"max-h-40 overflow-y-auto\">\n                  {result.errors.map((e, i) => <p key={i} class=\"text-xs text-red-300 mb-1\">{e}</p>)}\n                </div>\n              </div>\n            )}\n\n            <div class=\"mt-6 p-4 glass rounded-xl\">\n              <h3 class=\"text-sm font-semibold text-slate-300 mb-2\">CLI Scanner</h3>\n              <p class=\"text-xs text-slate-500 mb-2\">For a complete scan, run the CLI script from the project root:</p>\n              <code class=\"text-xs text-green-300 block bg-black/30 p-3 rounded-lg\">node scripts/scan-site.js</code>\n            </div>\n          </div>\n        );\n      }\n\n      ReactDOM.createRoot(document.getElementById(\"root\")).render(<App />);\n    ",
    "type": "text/babel"
  }
];

const BODY_HTML = `
    <div id="root"></div>
    
  

`;
const BODY_CLASS = "";

export const Route = createFileRoute("/admin")({
  component: AdminPage,
  head: () => ({
    meta: [
      { title: "CYNEX Admin" },
      { name: "description", content: "" },
      { property: "og:title", content: "CYNEX Admin" },
      { property: "og:description", content: "" },
    ],
  }),
});

function AdminPage() {
  useEffect(() => {
    const tag = "cynex-admin";
    const injected: Element[] = [];

    for (const href of STYLESHEETS) {
      const l = document.createElement("link");
      l.rel = "stylesheet";
      l.href = href;
      l.setAttribute("data-cynex", tag);
      document.head.appendChild(l);
      injected.push(l);
    }
    for (const css of INLINE_STYLES) {
      const s = document.createElement("style");
      s.textContent = css;
      s.setAttribute("data-cynex", tag);
      document.head.appendChild(s);
      injected.push(s);
    }
    for (const sc of SCRIPTS) {
      const s = document.createElement("script");
      if (sc.type) s.type = sc.type;
      if (sc.src) {
        s.src = sc.src;
        if (sc.async) s.async = true;
        if (sc.defer) s.defer = true;
      } else if (sc.content) {
        s.textContent = sc.content;
      }
      s.setAttribute("data-cynex", tag);
      document.body.appendChild(s);
      injected.push(s);
    }
    return () => {
      injected.forEach((el) => el.remove());
    };
  }, []);

  return <div className={BODY_CLASS}>{parse(BODY_HTML)}</div>;
}
